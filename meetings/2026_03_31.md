Date: 2026-03-31
Time: 15:00-16:00

### Completed Topics

1. Finalized the structure of the presentation slides.
2. Merged current and planned features into a single functional requirements slide, categorized by availability.
3. Decided to keep Vue.js in sequence diagrams to emphasize the technological stack and its role in asynchronous UI updates (progress bars, Redis job monitoring).
4. Reverted Docker configuration regarding Node/Vite volumes to a previous stable state to optimize container size and volume management.
5. Decided against a "detailed/non-detailed" checkbox; the system will proceed with Tshark for L7 analysis to ensure expandability without redundant analysis modes.

### Key Discussion Points

1.  Functional Requirements Updates:
    *   Add "Data Visualization" and "Display traffic flows" (renamed from "Follow a specific transmission").
    *   Highlight support for IP, IPv6, UDP, TCP, and DNS.
2.  UI/UX Improvements (Planned):
    *   Overview Tab: Add unique host sorting, protocol distribution pie charts, and packet size histograms (to detect ACK floods or large transfers).
    *   Packets Tab: Implement a Wireshark-like search bar (logical operators), better protocol-specific info (e.g., ARP "Who has..."), and lazy loading for performance.
3.  Backend and Performance Optimization:
    *   Asynchronous Processing: Proposal to implement asynchronous data streaming so L3-L4 results appear while L6-L7 are still processing.
    *   Multithreading: Explore splitting analysis into two threads (L3-L4 vs L6-L7) to improve speed.
    *   Data Storage: Use JSONB for L7 data; keep Python for DB insertions to manage RAM more efficiently during large file analysis.
    *   Redis: Consider using Redis for real-time progress percentage updates to reduce DB load.
4.  Testing and Simulation: Need to simulate concurrent analyses. Planned to use automated scripts or manual simultaneous uploads to test system stability.
5. Project Documentation (Report): Initial discussion on starting the formal report; need to review requirements or previous years' examples.
6.  Housekeeping:
    *   Delete the obsolete `scripts` folder.
    *   Consider adding `.cap` file support in the future (low priority).
