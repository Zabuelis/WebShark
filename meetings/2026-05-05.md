Date: 2026-05-05
Time: 13:00-14:00

### Completed Topics

1. Rate Limiting Implementation: Implemented Laravel’s native ⁠throttle middleware using Valkey. The system now uses a combination of Session and IP-based limiting to prevent abuse while accommodating "honest" users.
2. Cookie Error Handling: Added logic to detect if cookies are disabled and display an informative error message to the user.
3. Logging Bug Fix: Resolved a Laravel emergency log error related to the ⁠audit_stdout handler configuration.
4. "Hack the Project" Documentation: Prepared the initial version of the project documentation (PDF), including a dedicated section for hacking/extending the project.
5. Deployment Troubleshooting: Identified and fixed a 502 Bad Gateway error on the CI/CD VM by setting up a fresh environment and manually executing setup commands.

### Key Discussion Points

1. Decisions were made to include packets that do not have a TCP start (SYN flag) in the flow reconstruction. This ensures accuracy for captures where connections were already established before the recording started.
2. Packets Tab: Plans to implement Wireshark-style indicators (arrows/lines) to show where flows start, continue, and end.
3. Conversations Tab: Will act as a summary table (IP A, IP B, packet count, total size). Clicking a flow will redirect the user to the specific packets in the Packets Tab.
4. Recognized that the current setup uses ⁠npm run dev (Vite server) in the Docker environment. Suggested creating separate Docker configurations to use ⁠npm run build for production to improve stability.
5. Clarified the deadline for the final presentation.
6. Agreed to prioritize writing the project report in the coming week to gather feedback from supervisors while the MVP is already functional.
7. Discussed inconsistencies in how Chrome vs. Firefox handles ⁠navigator.cookieEnabled. Implemented a more robust check to ensure the cookie error message triggers correctly across different browsers.
