Date: 2026-02-24
Time: 16:00-18:00

### Completed Topics

1. Presentation Labor Division: Split the slides
2. Protocol Scope: Defined the initial analysis scope to L3 (IPv4, IPv6, ICMP) and L4 (TCP, UDP). Everything else (L7) will remain a hex dump for now.
3. Parsing Strategy: Confirmed using Scapy's haslayer() and getlayer() for abstraction instead of manual byte offsets.
4. Performance Optimization: Switched from rdpcap to PcapReader (lazy loading) to prevent RAM crashes on large files.

### Key Discussion Points

1. Database Pivot: Decided to scrap SQLite in favor of PostgreSQL. While SQLite is simpler to set up, its locking issues and poor scaling with multiple Python workers make it a bottleneck for this project.
2. Infrastructure (Docker): Agreed to move to a Docker Compose setup (Postgres, Redis, Laravel, Python, Nginx) to ensure environment parity.
3. Data Modeling: Discussed using UUIDs (generated in Laravel) as primary keys for Redis and DB entries to link jobs and analyzed packets consistently.
4. Permissions Management: Identified potential www-data permission issues in Docker and planned to use UID/GID mapping from the .env file to solve it.
5. Frontend/Inertia: will refactor the home page to properly use Inertia.js instead of basic Blade to keep the "Single Page App" feel.
