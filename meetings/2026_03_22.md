Date: 2026-03-22
Time: 18:00-19:00

### Completed Topics

1. Fixed the issue where Scapy script failures weren't triggering retries. Standardized the logic so Laravel workers handle retries, while Python marks jobs as failed.
2. Agreed to use `snake_case` globally across the project to avoid confusion with case sensitivity and abbreviations.
3. Resolved Vite installation issues in Docker. Added `setup.sh` scripts to automate repository cloning and environment configuration.
4. Merged the `dev` branch into `main` and verified that the `deploy_to_production` CI/CD pipeline is functional.
5. Updated the Entity Relationship diagram to include new fields from the `display_packets` branch.

### Key Discussion Points

1. Decided to use Scapy's internal L7 libraries instead of writing custom logic from scratch. While `PyShark` and `dpkt` were considered, Scapy remains the primary tool for now to maintain technical consistency.
2. Noted a ~40% performance decrease when switching from Redis-only storage to DB insertions. Further optimization is required, though initial findings suggest limited gains.
3. Talked about PostgreSQL `JSONB` for the `l7_data` column in the `packets` table. This allows flexible storage for different L7 protocols (HTTP, DNS, etc.) without inflating the table with null columns.
5. Job Lifecycle and Cleanup:
    *   `pending` -> `processing` -> `done` (expires in 2h) or `failed` (expires in 10m).
    *   A cron job will be implemented to clean up expired files and database rows.
6. Deployment and Demo:
    *   The "Copy Current URL" button on the production site is currently broken and needs a fix.
    *   Lazy loading or improved pagination is needed for the frontend, as the current implementation is inconvenient.
