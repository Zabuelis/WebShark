Date: 2026-03-04
Time: 12:00-13:00

### Completed Topics

1. Git Cleanup: Removed unnecessary "starter kit" components and synchronized .gitignore across branches to prevent tracking of local environment files.
2. Scapy Integration: Initial implementation of Scapy-based parsing for DNS, ICMP, ARP, IPv4, IPv6, TCP, and UDP protocols. 
3. Docker Environment: Successfully set up a Dockerized environment (Nginx, Redis, Laravel) tested on Mac and Debian VMs.
4. Network Configuration: Implemented SSH tunneling (ports 8000 and 5173) to access the project on VMs via localhost.
5. Nginx Fixes: Resolved the "413 Request Entity Too Large" error by updating the Nginx configuration to support files larger than 1MB.
6. CSRF Resolution: Refactored logic using Inertia.js to fix "419 CSRF token mismatch" errors during file uploads.

### Key Discussion Points

1. Decided to use Laravel Jobs with Redis for PCAP analysis instead of a standard REST API. This prevents timeouts and allows for better load management.
2. Discussed scalability using the --scale queue-worker=3 command, allowing multiple workers to pull tasks from Redis simultaneously.
3. Current flow: Laravel Job → Python script → Output to RAM (PHP Array) → Database. Raised concerns about memory bloat when processing large PCAP files. A future optimization may involve the Python script writing results directly to the database.
4. The current Vite server is for development only. For production, Vite will be replaced by a static build (npm run build) served directly by Nginx. Need to revisit Nginx "Security Headers" which are currently disabled due to Vite compatibility issues.	
5. Agreed to focus on a "Light Mode" first to save time, disabling automatic system-theme switching for now.
6. Plan to use Canva AI to generate layout ideas for the upcoming Home Page refactor.
7. Development of a "Setup Script" for fresh machines (automated Git, Docker, and environment configuration).
8. Implementation of PCAP data visualization and date formatting.
9. Basic Unit Tests are not needed right now.